# ================================================
# AUTO DEPLOY - WEB PLAYER
# Deploy automático via FTP + Auto Migrations
# ================================================
name: 🚀 Auto Deploy & Migrate

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permite execução manual

# Evita deploys simultâneos
concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: 📦 Deploy via FTP
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔑 Criar arquivo de chave de migration
        run: |
          echo "${{ secrets.MIGRATION_SECRET }}" > api/.migration_key

      - name: 🔄 Sincronizar via FTP
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq lftp > /dev/null 2>&1
          
          lftp -e "
            set ftp:ssl-allow yes;
            set ftp:ssl-force no;
            set ssl:verify-certificate no;
            set ftp:passive-mode yes;
            set net:timeout 30;
            set net:max-retries 3;
            set net:reconnect-interval-base 5;
            open -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }} ftp://${{ secrets.FTP_SERVER }};
            mirror --reverse --delete --verbose \
              --exclude-glob .git/ \
              --exclude-glob .git* \
              --exclude-glob .github/ \
              --exclude-glob api/.db.db \
              --exclude-glob api/adb.db \
              --exclude-glob api/config.php \
              --exclude-glob .DS_Store \
              --exclude-glob *.md \
              --exclude-glob .gitignore \
              --exclude-glob setup_secrets.py \
              ./ /;
            quit;
          "

      - name: 🗃️ Executar Auto Migrations
        run: |
          echo "⏳ Aguardando servidor estabilizar após deploy..."
          sleep 10
          
          echo "🔄 Executando migrations..."
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            -X POST \
            -H "X-Migration-Key: ${{ secrets.MIGRATION_SECRET }}" \
            "https://web.peepapp.xyz/api/migrate.php")
          
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -n -1)
          
          echo "📋 Resposta (HTTP $HTTP_CODE):"
          echo "$BODY"
          
          if [ "$HTTP_CODE" -ne 200 ]; then
            echo "⚠️ Migration retornou HTTP $HTTP_CODE"
            echo "Corpo: $BODY"
            # Não falha o deploy por causa de migrations
            # pois o banco pode já estar atualizado
          else
            echo "✅ Migrations executadas com sucesso!"
          fi

      - name: 📊 Resumo do Deploy
        run: |
          echo "## 🚀 Deploy Concluído!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Site:** https://web.peepapp.xyz" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Data:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "- **Autor:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
